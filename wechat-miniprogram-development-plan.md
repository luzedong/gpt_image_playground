# 「绘语造物」微信小程序开发计划

> 文档版本：v0.1
>
> 更新时间：2026-08-24
>
> 当前状态：方案设计
>
> 说明：产品名称、服务类目和合规结论在提交微信审核前仍需以平台实际审核结果及专业法律意见为准。

## 1. 项目目标

将当前 `gpt_image_playground` Web 应用，重构为可公开发布的微信小程序「绘语造物」，提供稳定、易用、可审核的 AI 图片创作能力。

首发版本聚焦“输入想法 → 生成/编辑图片 → 保存和管理”，不追求一次搬运 Web 端全部功能。

### 1.1 首发目标

- 支持文字生成图片。
- 支持上传参考图并进行图片编辑。
- 支持基础局部遮罩编辑。
- 支持生成任务进度、失败重试和历史记录。
- 支持保存图片、删除历史和问题反馈。
- 支持内容安全拦截、隐私政策和 AI 生成内容提示。
- API Key 由服务端管理，小程序端不暴露上游密钥。

### 1.2 首发暂不包含

- 用户自行填写任意 API 地址或 API Key。
- 公开作品社区、评论、点赞和关注。
- Web 搜索。
- 复杂 Agent 多轮工具调用。
- 全量外部 Prompt 图片素材库。
- 复杂的多图批处理和商业团队协作。

## 2. 产品信息

| 项目 | 暂定方案 |
|---|---|
| 小程序名称 | 绘语造物 |
| 小程序简称 | 绘语 |
| 产品定位 | 面向普通用户的 AI 图片生成与编辑工具 |
| 首选服务类目 | 工具 → 图片处理（以后台实际可选项为准） |
| 首发平台 | 微信小程序 |
| 推荐技术路线 | Taro/React + TypeScript + 自有后端 |
| 图像模型 | 服务端统一配置的合规图像模型 |

## 3. 推荐总体架构

不建议把当前 Web 页面直接嵌入小程序作为最终产品。推荐采用“小程序端 + 业务后端 + 图像模型服务”的结构：

```text
微信小程序
  ├─ 微信登录
  ├─ Prompt 输入
  ├─ 图片选择与上传
  ├─ Canvas 遮罩
  ├─ 任务进度与结果展示
  └─ 历史记录与反馈
          │ HTTPS
          ▼
业务后端
  ├─ 用户与权限
  ├─ Prompt / 图片内容安全
  ├─ 任务队列、超时、重试
  ├─ 图片存储与缩略图
  ├─ 历史记录与删除
  └─ 模型供应商适配层
          │ 服务端密钥
          ▼
图像模型服务（Pixel / OpenAI-compatible / 其他固定供应商）
```

### 3.1 前端技术建议

- 使用 Taro + React + TypeScript，延续当前项目的 React/TypeScript 经验。
- 页面和组件按小程序生命周期重写，不直接复用依赖 DOM 的 Web 组件。
- 使用 `wx.chooseImage`/`wx.chooseMedia` 选择图片。
- 使用小程序 Canvas 重写遮罩编辑器。
- 使用 `wx.uploadFile` 上传原图，避免在客户端长期保存大段 base64。
- 使用轮询或 WebSocket 获取长任务状态；首版优先轮询，降低部署复杂度。
- 使用 `wx.previewImage`、`wx.saveImageToPhotosAlbum` 完成预览和保存，并在需要时申请权限。

### 3.2 后端技术建议

- Node.js + TypeScript，复用当前项目的 API 适配经验。
- PostgreSQL 或 MySQL 保存用户、任务和审核记录。
- COS/S3 兼容对象存储保存原图、遮罩、结果图和缩略图。
- Redis/消息队列用于生成任务排队、超时和重试；MVP 可先使用数据库任务表。
- 服务端统一调用图像模型，按供应商适配 `/images/generations`、`/images/edits` 等接口。
- 对外只返回短期有效的图片 URL，不返回模型供应商的密钥和内部错误详情。

## 4. 从当前 Web 项目的迁移范围

| 当前 Web 能力 | 小程序实现方式 | 处理策略 |
|---|---|---|
| Gallery 画廊 | `pages/history` | 保留核心信息架构，重写列表和分页 |
| InputBar | `pages/create` | 拆成 Prompt、参数、上传和提交组件 |
| `submitTask` | 后端任务 API | 将模型调用、重试和持久化移到服务端 |
| IndexedDB | 数据库 + 对象存储 | 小程序只缓存轻量状态 |
| `MaskEditorModal` | Canvas 遮罩页/弹层 | 重写绘制、撤销、重做和导出 |
| AgentWorkspace | 后续版本 | 首发先不迁移 |
| Prompt 灵感库 | 精选、可授权的模板数据 | 首发可隐藏或只保留文字模板 |
| API Profiles | 服务端固定配置 | 小程序不开放任意 URL/Key |
| 下载/导出 | `wx.saveImageToPhotosAlbum` | 处理权限拒绝和失败提示 |
| Lightbox | `wx.previewImage` | 使用系统预览能力 |

当前项目中纯业务性的类型、参数校验和部分 API 适配逻辑可以抽取复用；依赖 `window`、`document`、IndexedDB 和 DOM 的部分需要重写。

## 5. 功能拆分

### 5.1 创建页

- Prompt 输入框和示例 Prompt。
- 选择生成尺寸和数量。
- 选择“生成图片”或“编辑图片”。
- 选择参考图、删除参考图、调整顺序。
- 进入遮罩编辑。
- 提交前显示预计等待和内容安全提示。

### 5.2 任务页

- 排队、生成中、成功、失败、已取消状态。
- 生成过程中的阶段提示。
- 失败后重试。
- 用户离开页面后可从历史记录恢复查看。

### 5.3 结果页

- 大图预览和缩放。
- 保存到相册。
- 继续编辑。
- 复制 Prompt。
- 删除结果。
- 显示“AI 生成”标识和必要的来源/免责声明。

### 5.4 历史页

- 按时间分页加载。
- 查看 Prompt、参数和结果。
- 删除单条记录。
- 批量删除可放到第二阶段。

### 5.5 账户与设置

- 微信登录状态。
- 用户协议和隐私政策。
- 图片/历史删除入口。
- 问题反馈与违规举报。
- 关于页面和版本号。

## 6. 后端接口草案

| 方法 | 接口 | 用途 |
|---|---|---|
| `POST` | `/api/auth/wx-login` | 微信登录换取业务会话 |
| `POST` | `/api/images/upload` | 上传参考图或编辑底图 |
| `POST` | `/api/tasks` | 创建生图/编辑任务 |
| `GET` | `/api/tasks/:id` | 查询任务状态 |
| `POST` | `/api/tasks/:id/retry` | 重试失败任务 |
| `POST` | `/api/tasks/:id/cancel` | 取消排队任务 |
| `GET` | `/api/history` | 分页获取历史记录 |
| `DELETE` | `/api/history/:id` | 删除任务及关联图片 |
| `POST` | `/api/reports` | 举报 Prompt 或图片 |
| `GET` | `/api/config` | 获取前端可展示的非敏感配置 |

任务状态建议统一为：

```text
created → moderating → queued → generating → succeeded
                                      └──────→ failed
                                      └──────→ cancelled
```

## 7. 开发阶段与里程碑

以下按一个熟悉 React/Node 的小团队估算，实际周期取决于后端、审核和模型接口准备情况。

### 阶段 0：产品和合规确认（3–5 个工作日）

- 确认名称、头像、简介和服务类目。
- 确认企业主体、微信小程序账号和备案路径。
- 确认图像模型供应商、数据处理位置和服务协议。
- 明确内容安全、图片标识、投诉举报和删除机制。
- 决定首发是否需要登录、是否限制免费额度。

交付物：产品说明、页面流程图、合规问题清单、供应商确认单。

### 阶段 1：交互和视觉原型（4–7 个工作日）

- 设计创建页、任务页、结果页、历史页和设置页。
- 设计空状态、排队状态、失败状态和权限拒绝状态。
- 设计深色/浅色策略，适配微信小程序导航和安全区域。
- 用真实手机尺寸验证 Prompt、上传和保存流程。

交付物：可点击原型、组件规范、页面验收标准。

### 阶段 2：后端基础能力（7–12 个工作日）

- 微信登录和用户会话。
- 数据库、对象存储和图片缩略图。
- 模型供应商适配层。
- 任务状态机、超时、重试和日志。
- 基础内容安全接口。
- 用户数据删除和后台运维入口。

交付物：可供小程序调用的 API、接口文档、测试环境。

### 阶段 3：小程序核心功能（7–12 个工作日）

- 创建页和 Prompt 输入。
- 图片选择、上传和预览。
- 生图任务提交和进度查询。
- 结果预览、保存和继续编辑。
- 历史记录和删除。

交付物：可运行的内部测试版。

### 阶段 4：遮罩和编辑能力（5–8 个工作日）

- Canvas 绘制、擦除、撤销和重做。
- 遮罩尺寸与透明度校验。
- 编辑请求上传 `image` 和 `mask`。
- 编辑失败恢复和再次提交。

交付物：参考图编辑闭环。

### 阶段 5：安全、隐私和审核准备（5–10 个工作日）

- Prompt 和图片生成前后审核。
- 违规提示、举报、封禁和人工复核流程。
- AI 生成内容标识。
- 用户协议、隐私政策、未成年人提示和数据删除说明。
- 审核账号、演示数据和审核说明。

交付物：审核材料包、风险演练记录、上线检查表。

### 阶段 6：灰度测试与发布（3–7 个工作日）

- 微信开发者工具、真机和弱网测试。
- 检查服务器域名、HTTPS、图片域名和权限弹窗。
- 小范围邀请体验，收集失败率和审核反馈。
- 提交审核，根据反馈进行整改。

交付物：线上首发版本、回滚方案、监控和客服流程。

### 阶段 7：第二阶段功能（审核稳定后）

- Agent 多轮对话和图片引用。
- 批量生成和任务组。
- 经过授权的 Prompt 模板库。
- 收藏夹、作品管理和跨设备同步。
- 会员、额度和团队协作。

## 8. 合规与审核清单

### 上线前必须确认

- 企业主体、账号认证和小程序备案状态。
- 小程序名称、头像、简介与实际功能一致。
- 服务类目与“图片生成/图片处理”功能匹配，以后台可选项为准。
- 所有生产请求使用已配置的 HTTPS 合法域名。
- 上游 API Key 仅保存在服务端。
- 有用户协议、隐私政策、账号/图片删除入口。
- 有 Prompt 和图片内容安全策略。
- 生成图片有 AI 生成内容说明或标识方案。
- 有举报、反馈和违规内容处理入口。
- 审核账号无需填写第三方 API Key 即可完成核心流程。
- 第三方 Prompt、案例图片和字体均有可核验授权；不直接复制未确认授权的外部图片素材。

### 审核演示流程

审核人员应能在 3 分钟内完成：

1. 登录小程序。
2. 输入一个安全的示例 Prompt。
3. 生成一张图片并查看进度。
4. 保存或预览结果。
5. 上传参考图并完成一次编辑。
6. 在历史页删除记录。
7. 找到用户协议、隐私政策和反馈入口。

## 9. 风险与应对

| 风险 | 影响 | 应对 |
|---|---|---|
| 任意 API/Key 模式无法解释 | 审核整改或安全事故 | 改为服务端固定供应商 |
| 违规 Prompt 或图片 | 下架、投诉、账号处罚 | 前后置审核、限流、举报和人工复核 |
| 模型响应超过小程序超时 | 用户误以为失败 | 后端异步任务 + 轮询/WebSocket |
| 图片占用过大 | 成本和加载慢 | 压缩、缩略图、对象存储和临时 URL |
| 第三方素材授权不清 | 版权投诉 | 首发只用自有/明确授权素材，保留来源 |
| Canvas 在低端机卡顿 | 编辑体验差 | 限制图片尺寸、分层绘制、导出前压缩 |
| 审核账号无法复现 | 审核失败 | 提供固定测试账号和安全示例数据 |

## 10. 验收标准

### 功能验收

- 生图成功率、失败率和平均耗时可统计。
- 生成失败可重试，不产生重复扣费或脏任务。
- 上传、编辑、遮罩和保存流程在主流 iOS/Android 真机可用。
- 删除历史后，数据库和对象存储中的关联数据按策略清理。
- 用户退出页面后，任务仍可在历史页查看最终状态。

### 质量验收

- 首屏核心内容在常见网络下可用，长任务有明确反馈。
- 无明显横向溢出，主要按钮满足触控尺寸。
- 无 API Key、内部 URL 或详细堆栈泄露到客户端。
- 生产日志不记录完整 Prompt、原图或敏感个人信息，除非有明确必要性和授权。
- 构建、单元测试、真机测试和审核演示流程全部通过。

## 11. 推荐执行顺序

```text
确认主体/类目/供应商
        ↓
后端固定模型与内容安全
        ↓
小程序创建页 + 生图闭环
        ↓
历史、保存、删除、反馈
        ↓
参考图编辑 + Canvas 遮罩
        ↓
审核材料与灰度发布
        ↓
审核稳定后再加入 Agent 和高级功能
```

## 12. 待决策事项

- [ ] 最终小程序名称是否确定为“绘语造物”。
- [ ] 是否采用 Taro，还是使用微信原生小程序。
- [ ] 后端部署位置、对象存储和数据库方案。
- [ ] 首发使用哪一家图像模型供应商。
- [ ] 是否需要登录后才能生成。
- [ ] 免费额度、并发限制和失败重试策略。
- [ ] AI 生成图片的标识形式。
- [ ] 是否在首发版本隐藏 Agent 和 Prompt 灵感库。

## 13. Definition of Done

只有同时满足以下条件，才认为首发版本完成：

- 小程序端、后端和对象存储已部署到生产环境。
- 服务器域名、HTTPS、备案和微信后台配置完成。
- 核心生图/编辑流程在真机通过。
- 内容安全、隐私、删除和举报流程可操作。
- 审核账号和审核说明准备完成。
- 监控、日志、告警和回滚方案已生效。
- 微信审核通过并完成一次线上灰度验证。
